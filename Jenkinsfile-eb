pipeline {
    agent any
    environment {
        AWS_ACCESS_KEY_ID = credentials('jenkins-access-key-id')
        AWS_SECRET_ACCESS_KEY = credentials('jenkins-secret-access-key')
    }
    
    stages {
        
        stage ("terraform init") {
            steps {
                sh ('terraform init') 
            }
        }
        
        stage ("terraform apply") {
            steps {
                sh ('terraform apply -target=module.aws-elasticbeanstalk.aws_elasticbeanstalk_application.sudos-duihua-app -target=module.aws-elasticbeanstalk.aws_elasticbeanstalk_environment.sudos-duihua-env --auto-approve')  
           }
        }
    }
}